using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cardTracker : MonoBehaviour {

    /**
	public Card[] cardArray;
	public int deckSize;
	public Card[] deck;
	int index;
    public GameObject thisDeck;

	// Use this for initialization
	void Start () {
		Card Array = new Card[thisDeck.n * thisDeck.n];
		index = 0;
		//deck = Generator.cards;

	}

	// Update is called once per frame
	void Update () {
		orderUpdate();
	}

	void orderUpdate() {
		foreach (Card card in deck) {
			//if (card.getChosen()) {
				//cardArray[index++] = card;
			}
		}
    */
}

